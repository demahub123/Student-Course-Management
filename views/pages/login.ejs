<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login - Student Course Management System</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Orbitron', monospace;
      background: radial-gradient(circle at top, #0d0f18 0%, #121622 80%);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      color: #fff;
    }

    .login-container {
      background: rgba(18, 22, 34, 0.8);
      padding: 50px 40px;
      border-radius: 20px;
      box-shadow: 0 0 40px rgba(0,255,247,0.5), 0 0 80px rgba(255,0,219,0.3);
      backdrop-filter: blur(20px);
      width: 90%;
      max-width: 400px;
      text-align: center;
    }

    .login-container h1 {
      font-size: 2.5rem;
      margin-bottom: 40px;
      color: #00fff7;
      text-shadow: 0 0 20px #00fff7;
    }

    .login-container input {
      width: 100%;
      padding: 15px;
      margin: 15px 0;
      border: none;
      border-radius: 10px;
      background: #0d0f18;
      color: #fff;
      font-size: 1rem;
      box-shadow: inset 0 0 10px rgba(0,255,247,0.2);
      transition: 0.3s;
    }

    .login-container input:focus {
      outline: none;
      box-shadow: 0 0 15px #00fff7, inset 0 0 10px #00fff7;
      border: 1px solid #00fff7;
    }

    .login-container button {
      width: 100%;
      padding: 15px;
      background-color: #ff00db;
      border: none;
      border-radius: 10px;
      font-size: 1.2rem;
      font-weight: bold;
      color: #121622;
      box-shadow: 0 0 20px #ff00db;
      cursor: pointer;
      transition: 0.3s ease;
    }

    .login-container button:hover {
      box-shadow: 0 0 40px #ff00db, 0 0 80px #ff00db;
      background-color: #ff33cc;
    }

  </style>
</head>
<body>

  <div class="login-container">
    <h1>Login</h1>
    <form action="/login" method="POST">
      <input type="email" name="email" placeholder="Enter your email" required>
      <input type="password" name="password" placeholder="Enter your password" required>
      <button type="submit">Login</button>
    </form>
  </div>

</body>
</html>
